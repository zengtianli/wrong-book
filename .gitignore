# Xcode 派生数据(可重生,别进仓)
.dd*/
DerivedData/
build/
.build-inputs/
*.xcuserstate
xcuserdata/

# xcodegen 从 project.yml 生成,不进仓 —— 进仓会和 project.yml 打架:
# 改了 yml 忘了重生成,工程和规格就悄悄不一致了。
WrongBook.xcodeproj/

# Generated public manifest is empty; private historical build exports stay in .build-inputs.
Resources/Lessons/*.html
Resources/Lessons/*.json
Resources/Lessons/primary-*/

.DS_Store
__pycache__/

# ── PNG 白名单 ──
# git-template 的 info/exclude 默认拦所有 *.png。图标是**构建输入**:actool 读它,
# 不进仓则 clone 下来编出来的 app 没图标。
!Resources/icon-1024.png
!Resources/Assets.xcassets/AppIcon.appiconset/icon-1024.png
# shots/ 是验收证据,几 MB,留本地不进仓;要复现: bash sim-run.sh
shots/

# CC 本地覆盖(可能含机器专属路径/开关)
.claude/settings.local.json

# ── 公开仓不带的内部件（本地仍在，只是不跟踪；oss_prep.py 维护）──
CLAUDE.md
catalog.yaml
.claude/
handoffs/
# README 截图白名单（git-template 的 info/exclude 默认拦 *.png）
!docs/screenshots/*.png
